-- Cell phone, captured on both the join.pangolinrc.com waitlist form and the
-- invest.pangolinrc.com "Request the deck" form. Additive; existing rows null.
ALTER TABLE waitlist ADD COLUMN phone TEXT;
